# ─── Buổi 3: Fine-tuning với LoRA/QLoRA ──────────────────────────────────────
# Tách khỏi requirements.txt chính vì:
#   - Nặng (torch + CUDA), chỉ cần khi TRAIN, không cần khi serve app.
#   - bitsandbytes yêu cầu GPU NVIDIA + CUDA → không cài được trên Mac/Windows-CPU.
#
# Cài (trên máy có GPU NVIDIA):
#   pip install -r training/requirements-train.txt

torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.9.0
datasets>=2.20.0
accelerate>=0.33.0
bitsandbytes>=0.43.0        # QLoRA 4-bit — CHỈ chạy trên GPU NVIDIA/CUDA
huggingface-hub>=0.24.0
