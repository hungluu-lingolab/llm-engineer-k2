# ─── Buổi 1: LLM APIs Hands-on ───────────────────────────────────────────────
openai>=1.40.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# ─── Buổi 4: Embeddings & Vector Databases ───────────────────────────────────
# Embedding dùng OpenAI (openai đã có ở trên). Vector store: Qdrant.
qdrant-client>=1.9.0            # vector store (HNSW, cosine; ":memory:" hoặc server)

# ─── Buổi 5: RAG Pipeline ────────────────────────────────────────────────────
pypdf>=4.2.0                    # đọc PDF (native, thay LangChain loader)
# Re-ranking (OPTIONAL — chỉ cần khi RAG_RERANK_ENABLED=true):
#   sentence-transformers>=3.0.0   # cross-encoder re-ranker

# ─── Dev / test ──────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ─── Các buổi sau sẽ bổ sung (giữ comment để thấy lộ trình) ───────────────────
# Buổi 5 (RAG Pipeline):
#   pypdf>=4.2.0              # document loader
#   langchain-text-splitters # chunking (hoặc tự viết)
# Buổi 6 (Agentic RAG):
#   langgraph>=0.1.0          # chỉ dùng để orchestrate; vẫn gọi native SDK
# Buổi 7 (Evaluation & Guardrails):
#   rouge-score, bert-score, nltk
# Buổi 9 (Capstone):
#   gradio>=4.0.0
