# ─── Buổi 1: LLM APIs Hands-on ───────────────────────────────────────────────
openai>=1.40.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# ─── Buổi 4: Embeddings & Vector Databases ───────────────────────────────────
# Embedding dùng OpenAI (openai đã có ở trên). Vector store: Qdrant.
qdrant-client>=1.9.0            # vector store (HNSW, cosine; ":memory:" hoặc server)

# ─── Buổi 5: RAG Pipeline ────────────────────────────────────────────────────
pypdf>=4.2.0                    # đọc PDF (native, thay LangChain loader)
# Re-ranking (OPTIONAL — chỉ cần khi RAG_RERANK_ENABLED=true):
#   sentence-transformers>=3.0.0   # cross-encoder re-ranker

# ─── Buổi 6: Agentic RAG (CRAG + Query Decomposition) ────────────────────────
langgraph>=0.2.0                 # CHỈ orchestrate control flow; mọi call vẫn native SDK
tavily-python>=0.5.0             # web search fallback (native, không qua LangChain)

# ─── Buổi 7: Evaluation, Guardrails & Monitoring ─────────────────────────────
langfuse>=2.0.0

# ─── Module II, Bài 2: Building Agents với LangGraph ─────────────────────────
# ToolNode/bind_tools cần interface LangChain — ngoại lệ có chủ đích so với
# "native SDK" (xem docstring app/agent_m2/nodes.py). langgraph đã có ở trên.
langchain-openai>=0.2.0
langgraph-checkpoint>=2.0.0      # MemorySaver (Section 5)

# ─── Dev / test ──────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ─── Các buổi sau sẽ bổ sung (giữ comment để thấy lộ trình) ───────────────────
# Buổi 9 (Capstone):
#   gradio>=4.0.0
